# 更新日志（Change Log）

本项目所有显著变更都会记录在此文件。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

当前处于 Windows MVP 开发阶段（M1、M2、M3 已完成，正在迭代增强与 M4 规划之间），尚未发布正式版本。

## [未发布] — 2026-08-06

### 新增（M3 进度与数据保障）

- **进度统计与热力图（FR-7）**：进度页今日概览（完成数/总数、今日已完成预估时长、目标剩余工作量）与最近 26 周「完成热力图」（按完成日期口径，色块 tooltip「日期 完成 N 项」+ 图例文本 0/1-2/3-5/6-8/9+，状态不只依赖颜色）；今天页负载卡同步展示完成统计行；无预估时长的任务只计入任务数（FR-7.4）。
- **手动备份/恢复（FR-9）**：设置页「备份与恢复」区，导出带版本号的 `.timecalc` 备份（manifest + JSON 文件 + zip 打包，单事务快照一致）；恢复前展示备份时间/目标数/任务数并要求确认「合并/覆盖」；覆盖恢复前自动创建当前数据安全副本（FR-9.3）并展示其路径；恢复先校验格式/版本/计数再原子生效，失败原库保持可用（NFR-2）。
- **关闭行为设置（FR-8.1）**：关闭按钮可配置为直接退出或最小化到托盘（schema v6 `Settings.close_behavior`），首次最小化到托盘时说明当前选择。
- **托盘（FR-8.2）**：系统托盘图标 +「显示主窗口 / 退出」菜单。
- **窗口位置/多屏恢复（FR-8.3）**：保存窗口尺寸、位置、最大化状态与显示器 id（独立 JSON 文件，不进入业务备份 FR-9.5）；显示器不可用时回退主屏可见区域，位置超出时约束到工作区。
- **性能基线（NFR-1）**：10,000 条任务数据下月历切换（查询 + 聚合）13ms ≤ 500ms 基线测试，单日查询 1ms ≤ 200ms。

### 修复

- 目标详情创建任务后「2026-08-06 · 2 小时」断言日期错位（表单默认下一可用日）。
- 今天页任务跨目标测试第二任务在视口外断言失败（滚动后再断言）。
- 设置页新增「关闭行为」保存按钮后，既有测试的 `find.text('保存')` 出现歧义（改取第一个）。

### 已知限制

- **检查项与里程碑（P1）**：任务检查项（FR-4.1）、目标里程碑（FR-2）未实现。
- **进度与数据保障（M3 已完成）**：燃尽趋势（FR-7.3 P1）、自动备份（FR-9.4 P1）、精简悬浮窗（FR-8.4 P1）与全局快捷键（FR-8.5 P1）未实现。
- **AI 计划草稿（P1 / M5）**：未实现。
- **重复任务（已交付）**：艾宾浩斯等间隔序列按天计算（同一天不产生多条同内容实例）；「仅修改模板」时已生成实例不自动重排；JSON 替换导入会停用该目标的重复模板。
- **数据存储位置**：数据库位于系统文档目录（`%USERPROFILE%\Documents\timecalc.sqlite`）；备份目录可配置随自动备份（FR-9.4）落地。
- **托盘图标**为程序化生成的简单图形，发布前可替换设计资源。

## [未发布] — 2026-08-05

### 新增（M1 数据与目标闭环）

- **项目初始化**：Flutter Windows 工程与架构骨架（drift + Riverpod + GoRouter），中文界面、浅色/深色主题（PR #1）。
- **本地数据层**：drift SQLite 数据库，Goals / Subjects / Tasks 三张表，UTC 时间戳与本地日历日期分离存储，schema v1 与 migration 模板（PR #2）。
- **目标管理**：创建、编辑、归档、删除；删除前二次确认并提示将连带删除其任务；标记已完成/已放弃（PR #3）。
- **科目管理**：目标的科目分组（增删、点击重命名）；删除科目时任务保留并解除归属（PR #4 / #6）。
- **任务管理**：增删改查、完成状态切换、预估时长 1～1440 分钟校验（PR #4）。
- **倒计时**：纯 Dart 规则，按本地日期计算「剩余 N 天 / 今天截止 / 已逾期 N 天」，覆盖跨日、跨年、DST 边界（PR #3）。
- **今天页**：进行中目标倒计时卡片（PR #3）。
- **科目任务页**：目标详情页按科目卡片列表展示，点击科目进入该科目的任务列表；未分类任务在详情页底部独立管理（PR #7）。
- **批量添加任务**：多行粘贴一次创建多个任务；日期策略支持「全部同一天」与「每 N 天一个」（套卷/真题递推）；统一预估时长并记忆上次输入；创建前实时预览数量、日期范围与总时长；单事务写入，失败整体回滚（PR #8）。
- **创建流程**：创建目标时可一次性添加多个科目（如考研四门课），创建成功后自动进入目标详情页引导继续配置（PR #6）。
- **里程碑验收记录**：M1「数据与目标闭环」验收记录归档至 `docs/milestone-records/M1-2026-08-05.md`。

### 新增（M2 每日执行闭环）

- **今日任务闭环**：今日任务列表（跨目标、标注目标名）、完成/编辑/延期（快捷默认延期至下一可用日/指定日期）、快速添加任务与空态（PRD §8）。
- **今日负载概览**：展示当日未完成任务预估时长与可用时长，超出时显示「超出 X 分」提示（不只依赖颜色）；时长显示人性化（120 分钟 →「2 小时」）。
- **日历视图**：手写月历（周一开头），每格展示任务数/完成数/预估时长与「超出」徽标，不可用星期置灰、今天高亮；点击选日展示当日任务（完成/编辑/延期/删除/添加），支持历史日期补录（FR-3.6）。
- **任务延期**：`Tasks.original_planned_date` 记录原计划日期（FR-3.3 验收）；延期保留内容、归属与预估时长。
- **次日未完成任务集中确认（FR-3.7）**：今天页横幅提示昨日及更早未完成任务，支持批量延期至下一可用日/指定日期/保留原日期（不自动改计划）。
- **计划偏好设置**：每日可用时长（小时/分钟步进器，点击数字可直接输入）与每周可用日；保存后今天页负载/日历/快捷延期即时生效。
- **目标详情负载区（FR-5.3/5.4）**：剩余任务时长、剩余可用天数、建议日均时长；建议日均超可用时长时显示计划风险与建议（只建议不自动改，FR-5.5）。
- **数据库 schema v2**：`Settings` 计划偏好表、`Tasks.original_planned_date`，含迁移与失败回滚测试。

### 新增（迭代增强）

- **JSON 导入任务**（批量添加升级版）：按科目分组与未分类任务隔离；自动校验（JSON 合法性、日期不得早于今天、非法日期拦截），错误带位置逐条列出；目标下不存在的科目自动创建；单事务写入。
- **JSON 导入替换/合并**：替换模式将当前任务归档到「历史任务」（可手动恢复），合并模式追加到现有计划；替换前二次确认。
- **历史任务区**：目标详情底部展示 JSON 替换时归档保留的旧任务，支持恢复回当前计划（schema v3 `Tasks.archived_at`）。
- **重复任务（FR-4，P1 提前交付）**：可扩展规则引擎（RecurrenceRuleHandler + Registry），内置每天/每周指定星期/每隔 N 天/间隔序列（默认艾宾浩斯 1,2,4,7,15,30）；「模板 + 实例」模型，仅预生成未来 30 天实例并滚动生成；支持可选结束日期、编辑规则（仅修改模板/仅修改未来实例二选一）、停止重复（历史实例保留）；schema v4。
- **删除目标后跨页刷新**：目标删除/完成/归档后，今天页、日历、详情与历史任务缓存统一刷新（FR-3 验收）。

### 修复

- 目标详情页无返回键：导航由 `go` 改为 `push`，恢复 AppBar 返回箭头（PR #5）。
- 编辑目标后详情页不刷新：编辑保存时同步刷新列表与详情数据源（PR #5）。
- 未分类任务区补充用途引导文案（科目复习/复盘、考研报名等），空态文案引导创建（PR #9）。
- 删除目标后今天页/日历仍显示其任务：改为 family 级统一刷新任务缓存。
- 日历「超出」数值跨页刷新不一致：今天页操作后同步刷新日历月数据。

### 修复（代码审查：P0 崩溃 + P1 功能 + P2 健壮性）

**崩溃级（P0）**
- 创建目标不选截止日期直接崩溃（`_deadline!` 空值解包）：截止日期改为 `FormField` 必填校验，未选择时展示错误而非崩溃。
- 删除被重复任务模板引用的科目触发外键约束异常：删除科目时同步置空模板的 `subject_id`。
- 编辑规则类型未注册的重复模板崩溃（SegmentedButton 断言）：未注册类型回退到默认规则。

**功能缺陷（P1）**
- 编辑任务无法清空备注：`TaskRepository.update` 的 `note` 改为 `Value<String?>?`，空备注显式置空落库。
- 重复任务编辑框的标题/科目/预估时长/起始日期此前不生效（`updateRule` 只更新规则）：现在写入模板，未来实例重生成时使用更新后的基础信息。
- 今天页逾期横幅被空态遮蔽：无活跃目标且无今日任务但有逾期任务时，横幅与任务区正常展示。
- 今天页空态快捷添加后新任务不出现：改为等待对话框保存完成后再刷新缓存。
- 任务表单/批量表单保存后只刷新本目标列表，今天页/日历/逾期横幅缓存陈旧：补全跨页 invalidate。

**健壮性（P2）**
- JSON 导入自动新建科目后科目列表不刷新：补 `subjectListProvider` invalidate。
- 删除目标后重复模板缓存未失效：补 `recurrenceTemplatesProvider` invalidate。
- 重复规则参数类型不安全（`as int` 遇非 int 抛 TypeError）：handler 改类型守卫，注册表外层 try/catch 兜底。
- `SettingsRepository.get()` 并发首次调用触发 UNIQUE 约束竞态：惰性 seed 改 `insertOrIgnore`。
- JSON 导入超长标题/科目名触发底层数据库异常：解析层校验标题 ≤200、科目名 ≤100。
- **删除的重复任务实例会「复活」**（滚动生成时按规则重新出现）：schema v5 为模板增加 `deleted_instance_dates` 墓碑列，删除实例时记录日期，`generateDue`/`updateRule` 跳过墓碑日期；迁移改为 drift step-by-step（各版本快照建表，杜绝累积迁移重复建列）。

### 已知限制

- **检查项与里程碑（P1）**：任务检查项（FR-4.1）、目标里程碑（FR-2）未实现。
- **进度与数据保障（M3）**：统计、热力图、手动备份/恢复、托盘与窗口恢复、性能基线未实现。
- **AI 计划草稿（P1 / M5）**：未实现。
- **重复任务（已交付）**：艾宾浩斯等间隔序列按天计算（同一天不产生多条同内容实例）；「仅修改模板」时已生成实例不自动重排；JSON 替换导入会停用该目标的重复模板。
- **数据存储位置**：数据库位于系统文档目录（`%USERPROFILE%\Documents\timecalc.sqlite`），可配置备份路径属 M3 范围。
