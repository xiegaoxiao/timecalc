# 更新日志（Change Log）

本项目所有显著变更都会记录在此文件。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

当前状态：v1.0.0（M3 里程碑：进度与数据保障）已发布；v1.1.0（M4 MVP 发布候选 + 迭代增强）为本发布候选，尚未发布正式版本；v1.2.0（M5 首个增强迭代：里程碑 FR-2）为本发布候选的迭代增强，尚未发布正式版本；v1.3.0（M6 迭代：任务检查项 FR-4.1）为本发布候选的迭代增强，尚未发布正式版本；v1.4.0（M7 迭代：燃尽趋势 FR-7.3）为本发布候选的迭代增强，尚未发布正式版本；v1.5.0（M8 迭代：自动备份 FR-9.4）为本发布候选的迭代增强，尚未发布正式版本；v1.6.0（M9 迭代：WebDAV 整库文件同步）为本发布候选的迭代增强，尚未发布正式版本；v1.7.0（M10 迭代：明暗主题切换）为本发布候选的迭代增强，尚未发布正式版本。

## [未发布] — 燃尽图白话化（UI 简化）

### 增强（进度页「剩余工作量趋势」卡片白话化）

- **标题去黑话**：「燃尽趋势」→「剩余工作量趋势」，「理想参考线」→「匀速参考线」（图例），「实际剩余」→「剩余工作量」。
- **一句话结论（副标题）**：由原「最近 30 天剩余预估时长与理想参考线（FR-7.3）」改为白话结论句，按状态组装四种文案——「过去 30 天消化了 X，还剩 Y」/「过去 30 天消化了 X，带时长的任务已全部完成」/「过去 30 天还没有完成任务，还剩 Y」/「当前没有剩余工作量」；新增 `StatisticsService.burndownWindowDoneMinutes`（窗口内已完成时长合计，口径与 `burndownSeries` 一致）。
- **图上标注「今天」**：X 轴最右端固定显示「今天」（日期节奏仍每 5 天一个，最右端优先）；此前最右端无标注，看不出图右侧就是今天。
- **页底说明白话化**：明确「最右端=今天，对应当前剩余」与灰色虚线的含义。
- **读屏语义同步**：燃尽图 Semantics label 由「燃尽趋势」改为「剩余工作量趋势」。

**测试**：statistics_service_test 新增 burndownWindowDoneMinutes 组 4 项（窗口内计入 / 窗口前不计 / 起点当日计入 / 无时长不计）；progress_page_test 适配文案断言并新增 1 项（X 轴「今天」标注 + 结论句数值）；因「今天」现也作为 X 轴标签出现在进度页（IndexedStack 保活隐藏分支），goal_load_section/calendar_view/today_page/recurrence_widget 的「今天」页导航 tap 限定到 `NavigationBar` 内避免歧义。全套 **482 项测试通过**（477 基线 + 5 新增）。

### 增强（进度页视觉语言统一，M12）

- **统一卡片样式**：抽出 `_SectionCard`（微透明边框 + 圆角 12 + 柔和阴影 + `Clip.none` 防 tooltip 裁剪），五张卡（计划偏好 / 今日概览 / 剩余工作量趋势 / 完成热力图 / 任务耗时图）全部套用——此前只有燃尽卡有边框圆角，其余走主题默认，五卡视觉不一致。内部仍是 `Card`，测试定位不受影响。
- **统一区块头**：抽出 `_SectionHeader`（主色图标 20px + 标题 + 可选副标题 + 可选右侧内容），四张统计卡标题结构一致；燃尽卡「当前剩余」大字作为 trailing 保留。
- **统一图例**：三套图例（燃尽圆点 / 热力图色块 / 耗时图色块）收敛为同一个 `_LegendDot`（圆角方块 12×12，可带描边），图例文字字号 11→10 统一，间距统一。
- **统一空态**：三份重复空态实现收敛为 `_ChartEmptyState`（outline 色 40px 图标 + 主文案 + 副文案），文案原样保留。
- **颜色收敛与深色适配**：主绿 `0xFF216E39`（原 5 处硬编码）与浅绿 `0xFF9BE9A8`（原 2 处）抽为共享常量；燃尽理想参考线硬编码浅灰 → 主题 `scheme.outline`（深色下自动提亮）；燃尽节点/图例白描边在深色下改用 `scheme.surface`；热力图月份标签补主题色（与星期标签一致）。
- **图标风格统一**：今日概览「已完成任务」由 filled 风格 `task_alt` 改为 outlined `check_circle_outline`，与另两项 outlined 图标一致。
- 卡片内边距统一：热力图由 16 提至 20，任务耗时图由 (16,24,16,20) 统一为 (20,24,20,28)（底部给 X 轴斜日期标签与 tooltip 留白）。

**测试**：现有断言全部为「文本/类型/值」层，视觉改动未破坏任何用例；progress_page_test 含两处 `takeException() isNull` 布局回归与宽窄窗断言仍全绿。全套 **482 项测试通过**。

### 增强（外观升级：主题体系，M13）

- **组件级主题定制（克制型生产力风，参照 ui-template 模板）**：`AppTheme` 由纯 M3 默认升级为全局组件主题——卡片（圆角 12 + 微边框 + 轻阴影，进度页既有卡片语言全局化）、页面底色（浅色改为浅灰 `#F6F6F6`，卡片留白更清晰）、AppBar（去表面色晕染与阴影）、按钮（统一圆角 10）、分割线、SnackBar（floating + 圆角）、对话框（圆角 16）。所有页面 `Card` 自动统一，无需逐页改。
- **语义色 token（`AppSemanticColors` ThemeExtension）**：新增警告（橙）/ 成功（绿）/ 信息（蓝）三组 文字色+容器色，浅深两套并随主题注册。应用点：日历超载格与今天页「超出可用时长」提示由红色改为**警告橙**（与逾期/错误红色区分）、「今天截止」倒计时由红色改为警告色（行动提醒，非错误）；目标列表与今天页倒计时语义同步。
- **共享组件公有化**：进度页私有 `_SectionCard`（样式已并入全局 CardTheme，组件删除）、`_SectionHeader` → `SectionHeader`、`_ChartEmptyState` → `ChartEmptyState`（新增可选 caption/CTA），铺到目标详情（里程碑/科目）与日历空态。
- **错误态统一**：外观/同步/计划偏好/科目任务 4 个子页的裸 `Text('加载失败：$error')` 改为 `AppErrorView`（图标 + 重试按钮）。
- **空态补全**：里程碑/科目/日历「这一天没有任务」等纯文字空态升级为「图标 + 引导文案」的统一空态（文案保留，PRD §8「提供首个操作」）。
- **seed 常量合并**：`kTimeCalcSeedColor` 统一 app_theme 与启动错误屏两处硬编码。

**测试**：contrast_test 扩展 12 项语义色对比度断言（warning/success/info 文字色与容器色对，浅深两套 WCAG AA ≥4.5）；全部视觉改动不触及文案/数值/类型断言。全套 **494 项测试通过**（482 基线 + 12 新增）。

### 增强（品牌视觉强化，M13 迭代）

- **品牌渐变**：新增 `kTimeCalcBrandDeep → kTimeCalcBrandBright`（同 seed 色相的深绿→提亮绿，模板 A「同色深浅表达层次」手法）。落地两处 hero：
  - **今天页倒计时卡**：渐变背景 + 白字（标题/倒计时/下一里程碑），成为「今日焦点」卡；
  - **目标详情头部**：标题、描述与倒计时同款渐变卡，与今天页视觉呼应。
- **统一路由过渡**：全局 `FadeForwardsPageTransitionsBuilder`，所有页面跳转淡入淡出（不再生硬切换）。

**测试**：现有断言不触及卡片颜色/渐变（倒计时卡仍可点击进入目标详情、文案未变）；全量 **494 项测试通过**。

## [1.7.0] — 2026-08-07

### 新增（M10：明暗主题切换，外观页落地）

- **明暗主题三选一（M10）**：外观页由占位改为可配置——「跟随系统 / 浅色 / 深色」三选一（Material `SegmentedButton`），随 Windows 明暗自动切换或强制指定；**点击即生效**（写库 + 整树换肤，无需单独保存/重启）。
- **应用层**：`TimeCalcApp` 由写死 `ThemeMode.system` 改为 watch `settings.themeMode`（取值与 `ThemeMode.name` 一致，未知值回退跟随系统）；`AppTheme.light()/dark()` 两套 ThemeData 复用既有实现（含 contrast_test 固化对比度）。
- **外观页 UI**：三选一分段按钮（点击即生效，无独立保存按钮；写库失败还原选择并提示）+ 「当前模式」预览卡（并排展示浅色/深色色板的表面/主色对比，高亮当前选择）+ 切换反馈 SnackBar。
- **设置页摘要**：外观菜单项 subtitle 由「主题切换（后续里程碑提供）」改为实时摘要（跟随系统/浅色/深色）。
- **数据层（schema v12）**：Settings 新增 `theme_mode` 列（默认 `system`）；`from11To12` 迁移 + drift 快照 v12 + migration 测试（v11→v12 / v1→v12 / 半迁移幂等）；主题模式是设备级外观配置，**不进入业务备份**（FR-9.5），覆盖恢复/同步拉取时保留本设备选择。

### 修复（任务日期选择器收紧到目标截止日）

- **任务日期可选范围收紧**：添加/编辑任务、批量添加、重复任务的日期选择器由「前后各 10 年」收紧为 **[今天, 目标截止日]**（区间外日期置灰不可选）；目标截止日已过时退化为只能选今天（不崩溃）。此前可选中截止日之后的日期，保存后任务从目标详情/日历静默消失且无提示。
- **保存守卫兜底**：选择器之外再加一层日期校验（任务表单 / 批量添加含递推边界 / 重复任务），越界时**明确提示**「任务日期不能早于今天 / 不能晚于目标截止日（…）」，不再静默失败；批量添加的递推（每 N 天一个）若把后续任务铺到截止日后同样被拦截。
- **编辑兼容**：编辑历史任务（计划日期早于今天）时仅当用户改动日期才校验，直接保存其他字段放行；日期选择器 initialDate 钳制到区间内避免 datepicker 断言崩溃。
- **历史补录不受影响**：日历选日面板仍可回填历史日期（FR-3.6）；收紧仅针对任务表单的手动日期选择。

**测试**：新增 task_form_date_range_test 4 项（正常目标区间、截止日=今天退化、逾期目标不崩溃、批量递推超截止拦截提示）。全套 **465 项测试通过**（461 基线 + 4 新增）。

### 增强（同步页：记住密码 + 连通性测试）

- **记住密码**：WebDAV 密码填过即经系统凭据存储加密保存（DPAPI，NFR-3）；密码框留空复用已保存密码，「保存并测试连接」与「立即同步」均可用；页面展示「密码已记住（系统凭据存储）」状态，提供「清除已保存密码」入口（清除凭据 + 重置标记）。
- **改地址重置密码标记（修复）**：此前改 WebDAV 地址后仍显示「已保存密码」但新地址实际无密码（误导）；现地址变更且未填新密码时自动重置「已保存」标记（旧地址密码仍保留供旧地址恢复使用）。
- **连通性测试**：新增「保存并测试连接」按钮——先只读探测（建同步目录 + 列目录，`WebDavSyncService.testConnection`）再保存配置，避免「保存了但连不上」；401 抛认证异常、403/网络错误转可读文案；密码留空时复用已保存密码测通。
- **按钮拆分**：原「保存设置」拆为「保存并测试连接」（先测后存）与「仅保存」（直接落库）。

**测试**：webdav_sync_service_test 新增 3 项（建目录+列目录成功、401 认证异常、403 无权限）；sync_page_test 扩展为 8 项（新增：测试连接成功保存、密码留空复用已保存测通、清除已保存密码、改地址重置标记）。全套 **472 项测试通过**（465 基线 + 7 新增）。

### 修复（自动备份开关不生效）

- **开关点击即写库（根因）**：此前「启用每日自动备份」开关拨动只改内存 state，必须另点「仅保存」才落库——用户拨完直接返回，`auto_backup_enabled` 仍是 false，调度器永远跳过「未开启」，即「开关没效果」。现开关**点击即写库**（不用再点保存），写库失败还原开关并提示。
- **开启后立即触发一次检查并反馈**：开关开启后马上 `run()` 一次并把结果直接反馈——成功显示「自动备份完成：N 个目的地」、跳过显示原因（如「距上次不足 24 小时已跳过」）、失败列错误；此前开关保存后干等最长 1 小时（调度器每小时复查）才见分晓。
- **WebDAV 密码缺失明确指认（潜在误导）**：此前配置了 WebDAV 地址/用户名但密码未保存时，跳过原因笼统报「未配置备份目的地」；现明确指出「WebDAV：未保存密码，请在「自动备份」页重新保存」。
- **仅保存语义收敛**：「仅保存」不再重复写入开关（已由开关点击即写库负责），只保存目的地相关配置。

**测试**：auto_backup_service_test 新增 1 项（密码缺失时跳过原因明确指认）；auto_backup_page_test 新增 2 项（开关点击即写库无需保存、开启后立即触发检查并反馈）。全套 **475 项测试通过**（472 基线 + 3 新增）。

### 精简（自动备份 WebDAV 目的地移除，M11）

- **自动备份只保留本地目录目的地**：WebDAV 数据保护由 M9「同步」承担（整库快照上传，含凭据），自动备份不再往 WebDAV 传，避免两套云端写入冗余。自动备份页移除 WebDAV 表单（地址/用户名/密码/保存并测试连接），文案注明「WebDAV 数据保护由同步承担」。
- **保留「从备份位置恢复」的 WebDAV 列表**：备份页仍列出本地目录 + WebDAV 的历史备份文件（含此前自动备份/手动上传的 `.timecalc`），可继续恢复——`buildEnabledTargets` 与 `WebDavBackupTarget` 保留。
- **`AutoBackupService.testWebDavConnection` 删除**：自动备份不再测 WebDAV 连接（同步页已有 `testConnection`）。
- **设置页摘要**：自动备份目的地摘要简化为「本地目录 / 未配置目录」（不再显示 WebDAV）。

**测试**：auto_backup_service_test 重写 2 项（自动备份只写本地不传 WebDAV、本地目录失败整体失败不推进时间戳）；auto_backup_page_test 删 2 项 WebDAV 用例、初始态断言「服务器地址」不再出现。全套 **471 项测试通过**（475 基线，净 -4）。

### 简化（设置页交互与结构，M11）

- **关闭行为页去「保存」键**：SegmentedButton 点击即写库 + 实时应用桌面层（`applyCloseBehavior`），写库失败还原选择并提示；切换弹轻量 SnackBar「已切换为…」。
- **同步页开关「即用即生效」**：总开关点击即写库（此前必须另点保存才落库，与自动备份开关同款问题）；删除「仅保存」按钮（保留「保存并测试连接」——测通才保存账号配置与密码）；同步本就是自动的（启动/变更/退出/每 5 分钟），开启即自动拉取/推送。
- **自动备份并入「备份与恢复」页**：删除独立「自动备份」页与菜单项/路由；「备份与恢复」页顶部新增自动备份区（总开关点击即写库 + 立即检查反馈、本地目录选择点击即写库、立即备份、最近备份时间），下方保留手动备份/恢复（导出/从文件恢复/从备份位置恢复）。

**测试**：auto_backup_page_test → 改名为 backup_page_test（pump BackupPage，覆盖自动备份区 + 手动区入口）；settings_page_test 关闭行为两用例改为「点击即写库 + 实时应用」；sync_page_test 删「仅保存」用例改为「开关点击即写库」、改地址用例改走「保存并测试连接」。全套 **471 项测试通过**（结构改动用例数持平）。

**测试**：新增 appearance_page_test 4 项（初始态默认跟随系统且无保存按钮、点击深色即写库+换肤 dark、点击浅色即写库+换肤 light、改回跟随系统即写库回 system）；settings_repository_test 新增 3 项（默认 system、浅色/深色持久化、互不覆盖）；migration_test 新增 3 项（v11→v12 / v1→v12 / 半迁移幂等）；backup_service_test 新增 1 项（覆盖恢复后主题模式保留）。全套 **461 项测试通过**（450 基线 + 11 新增）。

## [1.6.0] — 2026-08-07

### 新增（M9：WebDAV 整库文件同步，PRD §4.3「云同步」从远期落地）

- **整库文件同步（M9）**：以「整库快照」为同步单元，把全部业务数据（目标/科目/里程碑/任务/检查项/重复模板/计划偏好）同步到 WebDAV，多台设备共用同一份数据。远端目录 `webdav_sync/` 与自动备份目录 `webdav_auto/` 隔离，只保留一份最新快照 `timecalc-sync.latest.timecalc` + 元数据 `timecalc-sync.meta.json`（seq/时间/schema 版本，免下载大文件比较新旧）。
- **触发时机（四类 + 周期复查）**：启动拉取远端快照；业务表数据变更防抖 3s 后推送；退出应用前推送（5s 超时尽力而为，不阻塞退出）；设置页「立即同步」手动触发；运行中每 5 分钟复查远端（补足另一设备运行期间的变更）。
- **拉取守卫**：远端快照由更高 schema 版本生成时中止并提示升级（避免降级读坏）；拉取覆盖本地前自动创建安全副本（复用 FR-9.3）；运行时配置（关闭行为/自动备份/同步自身）各设备独立保留，不被拉取重置。
- **变更监听**：drift 无公开全局更新流，用 6 张业务表 `managers.<table>.watch()` 合并 + 防抖；**不监听 settings 表**（同步服务推送后写 seq/时间会触发回环，计划偏好由启动/周期/手动/退出推送覆盖）。
- **UI**：设置页新增「同步」菜单项；`/settings/sync` 独立页（启用开关 + WebDAV 账号表单（与自动备份共享账号，密码经系统凭据存储 DPAPI）+ 立即同步 + 最近同步时间 + 后写者胜语义说明）。
- **数据层（schema v11）**：Settings 表新增 `webdav_sync_enabled`/`last_pushed_seq`/`last_synced_at`（运行时配置，不进入业务备份，FR-9.5）；`from10To11` 迁移 + drift 快照 v11 + migration 测试（v10→v11 / v1→v11 / 半迁移幂等）；覆盖恢复保留同步 3 列（与 close_behavior/自动备份同模式）。

**测试**：新增 webdav_sync_service_test 8 项（未启用/无账号跳过、首次推送 seq 递增、远端较新拉取覆盖 + 同步开关保持、远端不快只推不拉、schema 版本守卫、拉取幂等不重复、并发互斥）；sync_page_test 4 项（初始态/保存持久化含密码/未启用跳过提示/推送成功反馈）；settings_repository_test 新增 4 项（同步配置读写/互不覆盖）；migration_test 新增 3 项（v10→v11 / v1→v11 / 半迁移幂等）；backup_service_test 新增 1 项（覆盖恢复保留同步配置）；既有 v8→v9 迁移用例改用原始 SQL 读取 settings（中间库缺 v11 列）。全套 **450 项测试通过**（430 基线 + 20 新增）。

### 修复

- **退出推送接线**：DesktopController 的 `onWindowClose`（exit 分支）与托盘「退出」两处统一走 `_quitApp`（先执行同步推送再销毁窗口）；minimizeToTray 分支不触发推送（不退出）。

## [1.5.0] — 2026-08-06

### 新增（M8：每日自动备份 FR-9.4 + 多目的地备份/恢复）

- **每日自动备份（FR-9.4）**：设置页新增「自动备份」独立页——启用开关 + 本地目录 + WebDAV 两个目的地；应用运行期间语义（启动即检查一次 + 每小时复查，距上次成功不足 24 小时跳过）；每个目的地只保留最近 7 份自动备份（前缀隔离，绝不删手动导出）；失败不推进时间戳并在当日去重提示。
- **WebDAV 目的地**：自研薄客户端（`http` 包提升为直接依赖，MKCOL/PUT/PROPFIND/GET/DELETE + Basic Auth，路径逐段 percent-encode 兼容非 ASCII 目录）；「保存并测试连接」只读探测（建目录 + 列目录）；401 抛认证异常、网络错误转可读文案。
- **凭据保护（NFR-3）**：WebDAV 密码经 `flutter_secure_storage`（Windows DPAPI）存系统凭据存储，不落业务数据库、不进备份文件（FR-9.5）；地址维度区分多套凭据；测试一律 override 假实现不触碰平台通道。
- **从备份位置恢复**：备份页新增「从备份位置恢复」——列出本地目录/WebDAV 上的备份文件（来源 + 文件名 + 时间 + 大小），选中后下载到临时文件，走原有「清单预览 + 合并/覆盖确认」全链路。
- **数据层（schema v9）**：Settings 表新增 6 个自动备份配置列（`auto_backup_enabled`/`local_backup_folder`/`webdav_url`/`webdav_username`/`webdav_password_saved`/`last_auto_backup_at`，全部带默认值或可空，旧库免回填）；`from8To9` 迁移 + drift schema 快照 + migration 测试（v8→v9 / v1→v9 / 半迁移幂等）。
- **覆盖恢复保留运行时配置**：`_overwriteRestore` 现在连同 6 个自动备份配置列一起保留（与 close_behavior 同模式，FR-9.5）；备份 JSON 依旧不含这些字段。

**测试**：新增 webdav_client_test 11 项（MockClient，不碰真实网络）、backup_target_test 8 项（本地/WebDAV 目的地）、auto_backup_service_test 9 项（跳过/成功推进时间戳/失败不推进/保留策略/多目的地）、auto_backup_scheduler_test 4 项（幂等/失败去重/成功重置/跳过不提示）、migration_test 新增 3 项（v8→v9/v1→v9/半迁移幂等）、settings_repository_test 新增 6 项、auto_backup_page_test 5 项、backup_service 回归（覆盖恢复保留自动备份配置）。全套 **416 项测试通过**（含既有 368 + M8 新增 48；其中既有日期敏感用例「目标详情添加任务并显示」在干净 HEAD 上即失败，与本次无关）。

### 依赖

- `http ^1.6.0` 由传递依赖提升为直接依赖（WebDAV 客户端传输）。
- 新增 `flutter_secure_storage ^11.0.0`（WebDAV 密码系统凭据存储，Windows DPAPI）。

### 修复

- **MKCOL 409 视为目录已存在（回归）**：部分 WebDAV 服务器（Nextcloud、NAS、Caddy 等）在目录已存在时返回 `409 Conflict` 而非标准的 `405`，「保存并测试连接」会误报「创建目录失败：目标已存在冲突（409）」。现 409/302 与 405 一同按幂等成功处理；目录真实可用性由随后的 PROPFIND/PUT 兜底验证。webdav_client_test 新增 409 用例。
- **PROPFIND 409 自动建目录重试**：「读取目录失败：目标已存在冲突（409）」来自 PROPFIND 分支——部分服务器对「刚创建但尚未就绪/不存在的目录」返回 409。现 `list` 遇 409 先 `ensureFolder` 再重试一次，仍失败才报错（异常携带 `statusCode` 供排查），不掩盖真实错误；webdav_client_test 新增「409 重试成功」与「重试仍 409 抛异常」两用例。
- **多级路径逐级建目录（核心修复）**：用户 WebDAV 地址携带非根路径（如 `https://dav.jianguoyun.com/dav/timecalc`）时，MKCOL 一次创建多层（`timecalc/webdav_auto`）会因中间父目录不存在而返回 409（RFC 4918 标准行为）。现 `ensureFolder` 沿完整 URL 路径**逐级 MKCOL**：首个段是用户认证根（跳过不试，避免 401/403），其余段依次创建，每段 2xx/405/409/301/302 视为已就绪——既解决坚果云/NAS 的 409，又不会在用户根目录上产生无效写操作。已用真实坚果云账户 + `dav/timecalc` 地址全流程验证（建目录 → 上传 → 列目录 → 清理），webdav_client_test 新增「多级 baseUrl 逐级创建」与「认证根不创建」两用例。

### 增强（今天页过期任务区块，FR-3.7 扩展）

- **过期任务区块**：红条下方新增浅红背景「过期任务」区块，逐条列出昨日及更早未完成任务（每行标注「原计划日期 · 已逾期 N 天」），复用 TaskTile 的完成/编辑/延期/删除操作；无过期任务时区块隐藏；与红条共用同一数据源，任何操作（红条批量延期、区块内完成/单任务延期）经统一刷新联动消失。today_page_test 新增 4 项（区块展示/隐藏/完成联动/单任务延期联动），全套 **426 项测试通过**。

### 修复（WebDAV 客户端裸 host 场景 + 延期 DST 隐患）

- **裸 host baseUrl 不建目录（回归修复）**：`ensureFolder` 此前无条件跳过首段（视为认证根），当用户 WebDAV 地址为裸 host（如 `https://dav.example.com`，无路径段）时目标目录作为首段被误跳过、从不创建。现仅当 baseUrl 自身带路径段时才跳过其首段；webdav_client_test 新增「裸 host 目标目录即首段必须创建」用例。
- **延期服务 DST 隐患**：`DeferService.nextAvailableDate` 改用 `addLocalDays`（纯日历加法）替代 `DateTime.add(Duration(days: 1))`，消除夏令时回拨日「+24h 仍落在当天」导致延期返回今天的问题（与 task_repository.batchCreate 同模式）。

### 重构（checklists §14 P3 后续优化项，8/8 完成）

- **统一跨页刷新 helper**：新增 `lib/core/providers/app_refresh.dart` 的 `invalidateAppData`（公共 7 provider）/ `invalidateTaskForms`（对话框 4 provider），消除 today_page / calendar_view / goal_list_page / backup_page 与 4 个任务对话框的复制粘贴 invalidate 块。
- **提取共享日期工具**：全库 15 处私有 `_parseDate`/`_parseLocalDate` 副本全部收敛到 `date_text.dart` 的 `parseLocalDate`（此前已收敛 1 处，本次清除剩余 14 处）。
- **DurationStepInput null 展示「未设置」**：`value == null` 时从「0 小时 0 分」改为「未设置时长」并禁用字段，与保存语义（0 分视为未设置）一致；task_crud_widget_test 两处用例同步适配。
- **删除死代码**：`QuickTaskFormDialog.defaultGoalId` 参数无任何调用方，已删除。
- **加载指示器统一**：subject_manager / milestone_section 的 `LinearProgressIndicator` 改为 `Center(CircularProgressIndicator())`，与全项目一致。
- **合并 TaskTile 与 _TaskTile**：两个重复任务行组件收敛为唯一 `TaskTile`（可选参数 `subjects`/`showPlannedDate` 吸收差异）；重复的删除/停止重复确认对话框收敛为共享函数；**顺手补齐目标详情页/科目页缺失的「延期至下一可用日 / 延期…」菜单**（FR-3.3 P0 能力此前因拷贝漂移缺失）；task_crud_widget_test 新增「目标详情页任务行菜单提供延期」回归用例。
- **CountdownService terminated days 归零**：已完成/放弃/归档目标的 `days` 由负数改为 0（label 恒返回「已结束」、3 处调用点均不展示，杜绝负数外漏）；countdown_service_test 补断言。
- **高频查询索引（schema v10，纯物理层）**：tasks 4 个索引（goalId+archivedAt / plannedDate / status+archivedAt / status+completedAt）、milestones / subjects / recurrence_templates / checklist_items 各 1 个；`from9To10` 迁移（`CREATE INDEX IF NOT EXISTS` 幂等）+ drift schema 快照 v10 + migration 测试（v9→v10 / v1→v10）。行数据不变，备份文件内容不受影响。未做 status 列 CHECK 约束（需重建表，风险高，留待后续）。

**测试**：migration_test 新增 2 项（v9→v10 / v1→v10）；webdav_client_test 新增 1 项；task_crud_widget_test 新增 1 项（延期补齐）；countdown_service_test 补 1 项断言；diagnostics_service_test 改为断言实际 schema 版本。全套 **430 项测试通过**。

## [1.4.0] — 2026-08-06

### 新增（M7：燃尽趋势 FR-7.3）

- **燃尽趋势（FR-7.3）**：进度页新增燃尽趋势区（今日概览之后、热力图之前），展示最近 30 天「剩余预估时长」随日期的变化，并提供理想参考线——实际剩余线（实线）+ 理想参考线（虚线，按最晚进行中目标截止日线性递减到 0）。
- **口径**：某日剩余 = 当前未完成时长和 + 窗口内完成日严格晚于该日的时长和（今日点 = 当前剩余，与 FR-7.1 一致）；无预估时长的任务不计入（FR-7.4）。纯计算（StatisticsService.burndownSeries），无 schema 变更。
- **图表**：CustomPaint 项目首个图表先例（网格线 + 实际实线 + 参考虚线，PathMetric 分段绘虚线）；每点 tooltip + 屏幕阅读器语义（NFR-4，不只依赖颜色）；X 轴每 5 天日期标签、Y 轴 0~max 刻度；底部图例；空态提示。

**测试**：statistics_service_test 新增 6 项（窗口边界/无完成水平/窗口内完成递减/理想线线性归 0/endDate 早于窗口起点为 0/无时长不计入）；progress_page_test 新增 2 项（有数据展示 tooltip 与图例、空态）。全套 **368 项测试通过**。

### 增强（燃尽图 fl_chart 视觉重构）

- **fl_chart 引入**：新增依赖 `fl_chart ^1.2.0`（项目首个图表库，替换燃尽图手写 CustomPaint；热力图/甘特图仍为手绘，不在本次范围）。
- **面积填充**：实际剩余线下方 from 深绿 28% 到透明的渐变色填充，图表立体感与专业度显著提升。
- **平滑曲线 + 节点优化**：两条线 `isCurved` 平滑过渡；实际线节点 FlDotCirclePainter 白色描边（半径 3.5），细节更精致。
- **参考线虚线 + 浅色网格**：理想参考线 dashArray 虚线；X/Y 轴每 25% 浅色虚线网格，增强读值与参考感。
- **坐标轴与留白**：X 轴每 5 天日期标签（SideTitleWidget），Y 轴 0~max 刻度并留 margin（maxY × 1.1）避免文字重叠；Y 轴标签改为中文时长文本。
- **信息增强**：卡片 Header 右侧新增「当前剩余」大字（燃尽核心信息直接呈现）；Card 增加微透明边框 + 柔和阴影 + 更大留白（padding 20）。
- **图例胶囊化**：底部图例改为圆点 + 文字胶囊（实际剩余白描边深绿点 / 理想参考线灰点），间距放宽。
- **入场动画**：TweenAnimationBuilder 驱动图表高度 0→100% 从底部向上生长（600ms easeOutCubic）。
- **悬停 tooltip + 读屏语义**：LineTouchTooltipData 自绘 tooltip（日期 + 剩余/理想时长）；图表整体 Semantics 语义标签（NFR-4，不只依赖颜色）。

**测试**：progress_page_test 燃尽用例改为断言 fl_chart LineChart 渲染 + Header「当前剩余」+ 图例；数据正确性由 statistics_service_test 6 项继续保证。全套 **368 项测试通过**。

### 增强（任务耗时图 fl_chart 重构）

- **改名与语义纠正**：原「任务耗时甘特图」实为**按目标×周的周时长堆叠条形图**（每格竖向条形=该周该目标时长），无任务时间跨度、非真正甘特图（Henry Gantt 定义要求横向连续条表达任务 start→end 排程），名不符实。更名为「**任务耗时图**」，副标题与底部说明同步更新。
- **fl_chart BarChart 重构**：一维以周为横轴（26 周：过去 12 + 当前 + 未来 13），跨目标合并为每周一根堆叠条——深色=已完成时长（底）、浅色=未来计划时长（上），仅当对应段 >0 时加入（杜绝空段）。保留时间趋势，能直接看出未来计划量。
- **图表细节**：Y 轴中文时长刻度（reservedSize 68 + space 12，沿用燃尽图修复后配置杜绝压线）；X 轴每 3 周日期标签；悬停 tooltip 按周反查重建「M/d 起一周：计划 X · 完成 Y」（规避 fl_chart getTooltipItem 拿不到 stack 段的限制）；浅色虚线网格；整体读屏语义（NFR-4）。
- **横向自适应**：宽屏铺满，窄窗（<26 周最小宽度）出现横向滚动。
- **数据模型不动**：`StatisticsService` 的 `goalGanttData`/`ganttWeekStarts` 与单测保留（语义仍是按目标按周聚合）。

**测试**：progress_page_test 甘特图用例适配——主测试改断言 BarChart 渲染 + 标题/图例；「行高随窗口拉伸」回归改为「宽度自适应」（窄窗横向滚动无溢出）；今日概览断言限定在卡片内（Y 轴刻度引入同名文本）。全套 **368 项测试通过**。

### 重构（设置页整宽菜单）

- **信息架构统一**：设置页原有四种不一致形态（关闭行为胶囊按钮 / 备份独立按钮 / 已归档任务折叠行 / 外观与快捷键纯文字）统一为**整宽长条形菜单**——每个菜单项（图标 + 标题 + 摘要 + chevron）点击 push 进入独立子页。
- **独立子页**：关闭行为页（保存后实时应用到桌面层）、备份与恢复页、已归档任务页（独立页平铺懒加载 + 空态）、外观/快捷键占位页；新增 5 条 `/settings/*` 顶层路由；「前往备份恢复」直达 `/settings/backup`。
- **归档区计数优化**：折叠态只查计数（COUNT 查询），展开/进入子页才加载全量列表。

### 修复（代码审查 P1/P2/P3）

- **P1 首页「下一里程碑」卡片陈旧**：`nextUpcomingMilestoneProvider` 派生自 `milestoneListProvider`，里程碑勾选完成/删除后卡片自动刷新；备份恢复后里程碑缓存整族失效。
- **P2 备份解包类型错误静默失败**：`.cast` 惰性转换改为逐元素显式校验，数组含非对象元素 / 顶层非数组 / 损坏清单统一抛 `BackupException`（此前抛 TypeError，UI 捕不到）。
- **P2 `kAppVersion` 与 pubspec 脱节**：`lib/core/app_version.dart` 单一来源 + 一致性测试（解析 pubspec 校验，防再次脱节）。
- **P3 `updateRule` 窗口重复重算**：future 应用后 `generatedThroughDate` 直接推进到目标。
- **P3 已归档目标误显「已逾期」**：`archived` 状态归入 `terminated`（显示「已结束」）。
- **P3 风格**：4 处 `switch` 补显式 `break`；schema 版本注释修正。

## [1.3.0] — 2026-08-06

### 新增（M6：任务检查项 FR-4.1）

- **检查项（FR-4.1，schema v8）**：任务可包含可排序的检查项。任务菜单「检查项…」打开检查项对话框——顶部输入行 +「添加」；列表按 sortOrder 展示，每行 Checkbox（勾选划线，不只依赖颜色）+ 上移/下移（同任务内交换排序）+ 重命名 + 删除（二次确认）。
- **完成任务二次确认**：任务存在未完成检查项时，勾选完成弹出确认（「还有 N 个检查项未完成」），确认后完成、取消保持原状态；无未完成检查项时保持原行为（既有流程不受影响）。今天页/日历选日面板与目标详情页/科目任务页两处任务行共用同一确认逻辑。
- **数据迁移 v7→v8**：新增 ChecklistItems 表（taskId/title/done/sortOrder），迁移幂等（createTable 自带 IF NOT EXISTS）；任务删除、JSON 替换导入删旧任务、目标级联删除时一并清理检查项（NFR-2 不留孤儿数据）；诊断导出行数段新增 checklist_items。
- **备份纳入**：检查项进入全量业务备份（manifest 计数 checklistItemCount、编解码、导出/合并（taskId 经旧→新任务 id 映射）/覆盖恢复）；旧版备份（无 checklist_items.json）恢复时按空处理兼容。

**测试**：迁移测试新增 v7→v8、v1→v8、半迁移幂等 3 项；checklist_item_repository_test 13 项（CRUD/上下移/越界/未完成计数/级联删除/导入替换）；checklist_widget_test 4 项（对话框管理/二次确认确认与取消/无检查项回归/_TaskTile 入口）；备份测试补检查项导出/覆盖/合并映射断言与 manifest 计数。全套 **360 项测试通过**。

## [1.2.0] — 2026-08-06

### 新增（M5 首个增强迭代：里程碑 FR-2）

- **里程碑（FR-2，schema v7）**：目标下可添加、编辑、标记完成和删除里程碑，完成状态用 Checkbox + 划线表达（不只依赖颜色，NFR-4），删除有二次确认；里程碑日期晚于目标截止日时保存前阻断并提示（FR-2.2）。目标详情页按「目标概览 → 里程碑 → 任务」层级展示完整里程碑列表（PRD §7）。
- **首页最近里程碑（FR-2.3）**：今天页每个目标卡片展示距离最近的一个未完成里程碑（「下一里程碑：{标题} · {日期}」）；里程碑全部完成或不存在时不显示该行。
- **数据迁移 v6→v7**：新增 Milestones 表，迁移幂等（createTable 自带 IF NOT EXISTS），删除目标时里程碑随目标级联删除（NFR-2 不留孤儿数据）；诊断导出行数段新增 milestones。
- **备份纳入**：里程碑进入全量业务备份（manifest 计数、编解码、导出/合并/覆盖恢复、恢复确认对话框展示里程碑数）；旧版备份（无 milestones.json）恢复时按空处理兼容。

**测试**：迁移测试新增 v6→v7、v1→v7、半迁移幂等 3 项；milestone_repository_test 9 项（CRUD/排序/nextUpcoming/级联删除）；milestone_widget_test 5 项（添加/编辑/阻断/完成/删除）；today_page_milestone_test 3 项（FR-2.3）；备份测试补里程碑断言与合并映射专项。全套 **316 项测试通过**。

### 增强（重复任务列表手风琴折叠，FR-4 迭代）

- **列表折叠**：目标详情页/科目任务页中同一重复模板的多个实例（≥ 2）合并为一张父卡片展示——标题 + 日期区间（如「2026-08-05 ~ 2026-08-14」）+ N 个任务 + 重复标记，不再平铺多行。
- **手风琴交互**：父卡片默认折叠；点击整卡或右侧展开图标向下展开子任务列表，子任务保留复选框/日期/更多菜单，视觉内缩（缩进 + 左缘引导线）表明隶属关系。展开/收起为瞬时切换（条件渲染，仅布局一次）：含几十个实例的子列表若用 AnimatedSize 逐帧重测量会造成明显卡顿，故不做高度动画，状态变化由图标旋转反馈。
- **性能（展开/收起与进页加载）**：任务列表改为 **sliver 形态 + SliverList 懒加载**——目标详情页/科目任务页外层 `ListView(children:)` 改为 `CustomScrollView`，任务区只构建视口内的任务行（含展开的重复实例行），进入详情页与展开/收起时不再一次性实例化几十个任务行。**迭代优化**：`TaskListSection` 改为自身返回单个 `SliverMainAxisGroup`（展开状态内部维护），点展开/收起只重建任务区、不波及页面其他区块（里程碑/负载/科目）；模板信息一次批量查询后按 id 取 map，消除「每张父卡片独立 watch 单个模板」的 N+1 数据库查询。懒加载回归测试：小视口展开 30+ 实例后实例化行数远小于实例总数。
- **性能（进入目标详情页）**：历史任务区（JSON 导入替换归档保留的旧任务）原为一次性全量渲染（归档任务可能多达数百条，全量构建导致进页卡顿——实测 304 条任务含 230 条归档时进页约 1078ms）；改为**懒加载 [SliverList]** 逐行构建（标题区 + SliverList.builder），只实例化滚动到可见的归档任务行，实测降至约 285ms（-73%）。
- **模板级操作**：父卡片「更多」菜单提供编辑重复规则、停止重复（模板已停止时隐藏）、删除整个重复（二次确认并提示将删除的实例数）；删除调用 `RecurrenceRepository.deleteWithInstances`（单事务删模板 + 全部实例，NFR-2）。子任务「删除」仍只删单个实例（保留墓碑语义）。
- **边界**：单实例模板不折叠，仍以普通任务行展示；模板停止后父卡片标记「已停止」；今天页/日历选日面板按单日查询，天然无多实例平铺，不受影响。

**测试**：recurrence_repository_test 新增 deleteWithInstances 2 项（连带删除 + 不影响其他模板）；recurrence_widget_test 新增折叠专项 8 项 + 懒加载回归 1 项；历史任务区懒加载回归。全套 **330 项测试通过**。

### 重构（历史任务语义：替换导入只归档已完成 + 归档区移入设置页）

- **替换导入语义**：JSON 替换导入时，未完成的旧任务**直接删除**（替换即弃旧，不再产生"历史"堆积），仅**已完成的旧任务归档保留**。此前把目标下全部未归档任务（含未完成/未开始）一律打归档标记，一次导入产生一大批与「已完成」语义错位的"历史任务"。
- **详情页移除历史任务区**：归档任务不再占用目标详情页底部空间（详情页专注当前计划），进入详情页加载也更轻。
- **设置页「备份与恢复」卡片**：统一数据管理——导出备份、从备份恢复、**已归档任务（N）**（默认折叠；展开后固定高度懒加载列表，每条显示标题 + 完成日期 + 计划日期 + 时长；「恢复」按钮把归档的已完成任务恢复回其目标当前计划，以完成态出现可取消勾选）。
- **文案**：替换确认框与结果 SnackBar 明确「删除 N 个未完成旧任务、归档 M 个已完成任务」。

**测试**：repository 替换导入用例重写（已完成归档/未完成删除/全未完成归档为空 2 项）；替换导入 widget 用例更新（确认框/SnackBar 新文案、归档只含已完成）；goal_load_section 改断言详情页无历史区；新增 settings_archived_section_test 3 项（计数/展开恢复/空态）。全套 **331 项测试通过**。

### 重构（计划偏好移至进度页入口 + 独立编辑页）

- **进度页入口卡**：顶部新增「计划偏好」卡，展示当前摘要（每日可用 X · 每周 N 天），点击进入独立编辑页——偏好是解读进度（今日概览完成率/剩余工作量）的上下文。
- **独立偏好页**：`/plan-preference` 承载完整计划偏好编辑（每日可用时长小时/分钟步进 + 每周可用日），保存写库并刷新；设置页不再占用空间，信息架构更整齐。
- **设置页精简**：移除「计划偏好」区块，只保留关闭行为 / 备份与恢复 / 外观 / 快捷键。

**测试**：进度页入口卡断言 1 项；新增 plan_preference_page_test 3 项（时长保存/可用日保存/0 分钟阻断）；goal_load_section 偏好用例改走进度页入口路径；settings_page 关闭行为用例适配（区块前移）。全套 **335 项测试通过**。

### 已知限制

- **里程碑（FR-2）**：本版本已交付。
- **进度与数据保障后续（P1）**：燃尽趋势（FR-7.3）已交付（v1.4.0）；自动备份（FR-9.4）、精简悬浮窗（FR-8.4）与全局快捷键（FR-8.5）未实现。
- **AI 计划草稿（P1 / M5）**：未实现。

## [1.1.0] — 2026-08-06

### 增强（进度页自适应布局重构）

- **页面容器自适应**：整页改为 `LayoutBuilder + SingleChildScrollView`，内容横向拉伸铺满窗口约 95%（两侧约 5% 边距），消除宽屏左右大量留白。
- **今日概览卡片**：三项数据（已完成任务、已完成时长、目标剩余工作量）由纵向堆叠改为 `LayoutBuilder + Wrap` 等宽三列，宽屏横向撑满卡片，窄屏自动换行不挤压。
- **完成热力图**：`LayoutBuilder` 按卡片宽度动态计算色块尺寸（26 周 + 周间距恰好铺满），宽屏下色块从 12px 自动放大填满卡片；窄窗口回落到 6px 下限并出现横向滚动。
- **任务耗时甘特图**：按开源 `flutter_gantt` 布局重构——sticky 目标名列、双刻度表头（月份 + ISO 周序号）、网格线、每目标一行每周一格；周格宽按可用宽度等分（宽屏铺满消除右侧留白，窄窗口回落最小格宽横向滚动）；**行高随窗口高度与目标数自适应**（窗口高 × 45% ÷ 目标数，夹取 56~240px），窗口拉高条形变高，目标多时回落最小行高并靠整页滚动查看全部行；条形高度随行高增长（不再绑定死高度）。
- **测试**：进度页新增「宽屏 1600×900 卡片铺满无溢出」「窗口 600→900 甘特图行随纵向拉伸且整页可滚动到底部说明」布局回归测试；全套 **295 项测试通过**。

### 修复（代码审查：P0 状态失控 + P1 数据一致性 + P2 健壮性）

**崩溃/状态失控（P0）**
- **时长步进长按 Timer 失控**：`DurationStepInput` 的长按一次性 Timer 未跟踪，长按不足 400ms 即松开后周期性自动步进仍会启动，数值持续自增/自减。修复同时发现真正根因是手势层——`GestureDetector` 包裹 `IconButton` 时 InkResponse 抢占手势 arena，`onLongPressStart` 永不触发，原长按连续步进在 Windows 上本就不生效；改为自绘步进按钮（GestureDetector + Tooltip），一次性/周期 Timer 统一跟踪，短长按松开后不再持续步进。
- **延期日期选择器 `initialDate` 越界**：计划日期早于 `firstDate`（逾期一年以上的未完成任务）时，debug 下 datepicker 断言崩溃、release 下初值错误；`initialDate` 钳制到选择器下界。

**数据一致性（P1）**
- **覆盖恢复静默重置「关闭行为」**：备份按 FR-9.5 不含 `close_behavior`（桌面层状态），但覆盖恢复会清空重建 settings 行，导致最小化到托盘行为被重置为默认退出；恢复时读取并保留恢复前的 `close_behavior`（备份 JSON 若携带则以备份为准）。
- **备份恢复后详情/科目/重复任务页显示陈旧数据**：`BackupSection` 恢复后的跨页刷新遗漏 family 型缓存；补 `goalDetailProvider` / `subjectListProvider` / `recurrenceTemplatesProvider` / `recurrenceTemplateProvider` 整族失效。
- **重复任务结束日期可早于起始日期**：结束日期选择器下界固定为起始日，起始日改动后移时自动清空结束日，保存前兜底校验，不再静默生成 0 个未来实例。
- **批量添加间隔天数「所见≠所存」**：非法输入（空/0/负值）静默保留旧 `_intervalDays`，预览与实际保存不一致；改为输入框 validator + 保存/预览均以输入框实时解析。
- **间隔序列规则语义修正**：offsets 此前按「相对间隔累计」解释，默认 1,2,4,7,15,30 实际产生 +1/+3/+7/+14/+29 天，与 UI hint「第 1、2、4、7、15、30 天复习」矛盾；改为按「距起始日的绝对天数」解释，实现与提示一致。

**健壮性 / 可访问性（P2）**
- 路由畸形参数（非数字 `goalId/subjectId`，外部深链）由 redirect 重定向到首页，不再于 build 途中抛 `FormatException` 红屏。
- 窗口恢复尺寸下界钳制（≥400×300），损坏的 `window_state.json` 不再恢复出 0/负尺寸的不可见窗口。
- 全局错误处理器对 `diagnostics.capture` 加异常兜底，防止诊断服务自身抛错时无限递归。
- 快速添加任务对话框：过期的 `defaultGoalId` 回退到首个可用目标，防 Dropdown `initialValue` 与 items 不匹配断言。
- 编辑任务/编辑重复规则对话框取消后不再无条件触发跨页刷新（对话框返回保存结果，仅成功时刷新）。
- 任务完成复选框补 `semanticLabel`（NFR-4，读屏可读名称）。
- 数据层日期加法收敛到 `core/utils/date_text.dart`（纯日历加法，避免夏令时切换日 `Duration(days:)` 偏移一小时导致日期错位）。

**测试**：新增回归测试 7 项——时长步进长按连续/短长按不失控 ×2、覆盖恢复保留 close_behavior、路由畸形参数 redirect ×2 + 合法参数无回归、窗口恢复尺寸下界钳制；间隔序列相关断言（service/repository/widget 三处）同步更新为绝对复习日语义。全套 **293 项测试通过**。

### 新增（M4 MVP 发布候选）

- **数据库异常处理（PRD §8 / NFR-2）**：
  - 全局错误处理器（`FlutterError.onError` + `PlatformDispatcher.onError`）将未预期错误写入本地诊断日志（内存环 + 应用支持目录文件，容量封顶自动截断，NFR-3），不静默丢失；
  - 写入失败守卫：所有任务/目标/科目/重复任务/设置等数据库写操作统一走 `runDbAction`，失败时弹「数据保存失败」对话框——说明本次写入未生效（drift 事务已回滚，无半条写入）、应用已停止继续写入，并提供「导出诊断信息」与「前往备份恢复」两个自助入口；
  - 诊断导出（`DiagnosticsService`）：应用版本、schema 版本、数据库路径、各表行数（守卫读取）与最近错误日志，供排查或提交；
  - 启动失败兜底：数据库无法打开时不再裸崩溃，运行「数据无法打开」启动错误屏（说明、数据库路径、从备份恢复指引、导出诊断入口）。
- **读取失败统一错误视图**：各页 `加载失败：$error` 裸文本替换为 `AppErrorView`（图标 + 错误消息 + 重试按钮，invalidate 对应 Provider）；目标详情页内部区块静默 `SizedBox.shrink` 错误态一并补为可见错误提示。
- **无障碍（NFR-4 / checklists §8）**：
  - 日历「超出」格在红色文本外附警告图标，状态不只依赖颜色；
  - 屏幕阅读器语义：日历格（日期/完成数/时长/超出）、热力图色块（日期 + 完成项数）、甘特图条形（周起始 + 计划/完成时长）；
  - 主题对比度：浅色/深色主题关键色对全部 ≥ 4.5:1（WCAG 2.1 AA），由 `contrast_test` 固化（正文 16.3 / 14.3，错误 6.2 / 10.9，容器对 7.2 等）；
  - 核心流程键盘可达：Tab 可达「添加任务」等主按钮（focus 可达性 widget 测试）。
- **日历拖动改期（FR-5.1，P0 补齐）**：选日面板中的任务长按即可拖到月历网格任意日期改期；拖动悬停时目标格高亮；已完成任务不可拖动；改期复用延期语义并记录原计划日期（FR-3.3 验收）；失败时弹数据库错误对话框、任务保持原日期。
- **Windows 发布资产**：
  - `timecalc.exe` 版本元数据：ProductName/FileDescription「TimeCalc 时间计算器」、CompanyName「TimeCalc」、简体中文 StringFileInfo 块（Runner.rc + 窗口标题，UTF-8 BOM 保证 MSVC 中文编译）；
  - `tool/create_signing_cert.ps1`：自签名代码签名测试证书生成（导出 PFX 至 `build/signing/`，证书不入库）；
  - `tool/release.sh`：`flutter build windows --release` → signtool 自签名（SHA256 + 时间戳，失败不阻断）→ 整理发布目录（exe + dlls + data + LICENSE + CHANGELOG + SIGNING-NOTE）→ zip 打包（PowerShell Compress-Archive）→ 生成 SHA256SUMS。
- **发布演练**：自签名 + 签名验证 + zip + SHA256 校验通过；Windows 构建冒烟启动正常。

### 修复

- **目标删除/标记完成后 SnackBar 二次出现（P2）**：`runDbAction` 失败早退后，`_handleAction` 中残留的 `return` 缺失会继续弹成功提示；已补 `if (!ok) return`。
- 启动流程：`AppDatabase.open()` 不再裸抛崩溃，失败进入启动错误屏。

### 已知限制

- **代码签名**：当前为自签名测试证书（非受信 CA），Windows SmartScreen 仍可能提示；正式发布渠道要求 Authenticode 签名时，改用受信任代码签名证书后重跑 `tool/release.sh`（流程不变）。
- **DPI**：100/125/150/200% 各档未逐档实测（manifest 已声明 PerMonitorV2），发布前补充。
- **无安装器**：当前发布形式为便携 zip（按 M4 决策），MSIX/安装器待发布渠道确认后补充。
- **托盘图标**为程序化生成的简单图形，发布前可替换设计资源。
- **检查项与里程碑（P1）**：任务检查项（FR-4.1）、目标里程碑（FR-2）未实现。
- **进度与数据保障后续（P1）**：燃尽趋势（FR-7.3）、自动备份（FR-9.4）、精简悬浮窗（FR-8.4）与全局快捷键（FR-8.5）未实现。
- **AI 计划草稿（P1 / M5）**：未实现。

## [1.0.0] — 2026-08-05

### 新增（M3 进度与数据保障）

- **进度统计与热力图（FR-7）**：进度页今日概览（完成数/总数、今日已完成预估时长、目标剩余工作量）与最近 26 周「完成热力图」（按完成日期口径，色块 tooltip「日期 完成 N 项」+ 图例文本 0/1-2/3-5/6-8/9+，状态不只依赖颜色）；今天页负载卡同步展示完成统计行；无预估时长的任务只计入任务数（FR-7.4）。
- **手动备份/恢复（FR-9）**：设置页「备份与恢复」区，导出带版本号的 `.timecalc` 备份（manifest + JSON 文件 + zip 打包，单事务快照一致）；恢复前展示备份时间/目标数/任务数并要求确认「合并/覆盖」；覆盖恢复前自动创建当前数据安全副本（FR-9.3）并展示其路径；恢复先校验格式/版本/计数再原子生效，失败原库保持可用（NFR-2）。
- **关闭行为设置（FR-8.1）**：关闭按钮可配置为直接退出或最小化到托盘（schema v6 `Settings.close_behavior`），首次最小化到托盘时说明当前选择。
- **托盘（FR-8.2）**：系统托盘图标 +「显示主窗口 / 退出」菜单。
- **窗口位置/多屏恢复（FR-8.3）**：保存窗口尺寸、位置、最大化状态与显示器 id（独立 JSON 文件，不进入业务备份 FR-9.5）；显示器不可用时回退主屏可见区域，位置超出时约束到工作区。
- **性能基线（NFR-1）**：10,000 条任务数据下月历切换（查询 + 聚合）13ms ≤ 500ms 基线测试，单日查询 1ms ≤ 200ms。

### 修复

- **关闭行为变更不实时生效（P1）**：设置页切换「退出 ↔ 最小化到托盘」并保存后，`windowManager.setPreventClose` 不会重新应用，需重启才生效。`DesktopController` 新增公开 `applyCloseBehavior()`，设置页保存后调用，使切换实时生效（FR-8.1）。
- **甘特图最忙周条形溢出 ~3px（P2）**：`_heightFor` 对计划与完成两段各叠加固定基础偏移，最忙周两段合计超出外层 SizedBox 高度，触发 `RenderFlex overflowed`。改为纯比例映射到 `maxBarHeight - minBarHeight`，最忙周两段之和恰为满高，贴底对齐不溢出。
- 目标详情创建任务后「2026-08-06 · 2 小时」断言日期错位（表单默认下一可用日）。
- 今天页任务跨目标测试第二任务在视口外断言失败（滚动后再断言）。
- 设置页新增「关闭行为」保存按钮后，既有测试的 `find.text('保存')` 出现歧义（改取第一个）。
- **半迁移数据库打开崩溃（P0）**：`user_version` 落后但列已存在（如手工补列或迁移中断后重试）时，`ALTER TABLE ADD COLUMN` 抛「duplicate column name」。所有加列迁移改为幂等（先查 `PRAGMA table_info`，列已存在则跳过），迁移可重复/可恢复（NFR-2）；新增「v5 结构 + v4 版本号」半迁移回归测试。

### 新增（M3 迭代增强：进度可视化）

- **热力图 LeetCode 风格**：配色替换为 LeetCode 官方色板（#EBEDF0 → #9BE9A8 → #40C463 → #30A14E → #216E39），分桶对齐 LeetCode（0 / 1-3 / 4-6 / 7-9 / 10+）；色块小圆角 + 白间距、暗色主题空档深灰；tooltip「yyyy-MM-dd：完成 N 项」；图例改为紧凑横向（色块 + 分桶文本一一对应）。
- **任务耗时甘特图**：进度页新增甘特图 Card——X 轴为「过去 12 周 + 当前周 + 未来 13 周」（横向可拖拽，能看到之后的任务计划），Y 轴按目标分组（仅显示有计划/完成记录者）；每周时长分「浅绿=未来计划（未完成任务按计划日期归周）、深绿=已完成（按完成日期归周）」双段条形，高度按全局最大周时长归一化；悬停展示「周起始 yyyy-MM-dd：计划 X · 完成 Y」；底部图例「计划 / 完成」。
- 进度页整页纵向滚动（ScrollView），两张图表堆叠时小屏不挤压。

### 已知限制

- **检查项与里程碑（P1）**：任务检查项（FR-4.1）、目标里程碑（FR-2）未实现。
- **进度与数据保障（M3 已完成）**：燃尽趋势（FR-7.3 P1）、自动备份（FR-9.4 P1）、精简悬浮窗（FR-8.4 P1）与全局快捷键（FR-8.5 P1）未实现。
- **AI 计划草稿（P1 / M5）**：未实现。
- **重复任务（已交付）**：艾宾浩斯等间隔序列按天计算（同一天不产生多条同内容实例）；「仅修改模板」时已生成实例不自动重排；JSON 替换导入会停用该目标的重复模板。
- **数据存储位置**：数据库位于系统文档目录（`%USERPROFILE%\Documents\timecalc.sqlite`）；备份目录可配置随自动备份（FR-9.4）落地。
- **托盘图标**为程序化生成的简单图形，发布前可替换设计资源。

## [1.0.0] — 2026-08-05

### 新增（M1 数据与目标闭环）

- **项目初始化**：Flutter Windows 工程与架构骨架（drift + Riverpod + GoRouter），中文界面、浅色/深色主题（PR #1）。
- **本地数据层**：drift SQLite 数据库，Goals / Subjects / Tasks 三张表，UTC 时间戳与本地日历日期分离存储，schema v1 与 migration 模板（PR #2）。
- **目标管理**：创建、编辑、归档、删除；删除前二次确认并提示将连带删除其任务；标记已完成/已放弃（PR #3）。
- **科目管理**：目标的科目分组（增删、点击重命名）；删除科目时任务保留并解除归属（PR #4 / #6）。
- **任务管理**：增删改查、完成状态切换、预估时长 1～1440 分钟校验（PR #4）。
- **倒计时**：纯 Dart 规则，按本地日期计算「剩余 N 天 / 今天截止 / 已逾期 N 天」，覆盖跨日、跨年、DST 边界（PR #3）。
- **今天页**：进行中目标倒计时卡片（PR #3）。
- **科目任务页**：目标详情页按科目卡片列表展示，点击科目进入该科目的任务列表；未分类任务在详情页底部独立管理（PR #7）。
- **批量添加任务**：多行粘贴一次创建多个任务；日期策略支持「全部同一天」与「每 N 天一个」（套卷/真题递推）；统一预估时长并记忆上次输入；创建前实时预览数量、日期范围与总时长；单事务写入，失败整体回滚（PR #8）。
- **创建流程**：创建目标时可一次性添加多个科目（如考研四门课），创建成功后自动进入目标详情页引导继续配置（PR #6）。
- **里程碑验收记录**：M1「数据与目标闭环」验收记录归档至 `docs/milestone-records/M1-2026-08-05.md`。

### 新增（M2 每日执行闭环）

- **今日任务闭环**：今日任务列表（跨目标、标注目标名）、完成/编辑/延期（快捷默认延期至下一可用日/指定日期）、快速添加任务与空态（PRD §8）。
- **今日负载概览**：展示当日未完成任务预估时长与可用时长，超出时显示「超出 X 分」提示（不只依赖颜色）；时长显示人性化（120 分钟 →「2 小时」）。
- **日历视图**：手写月历（周一开头），每格展示任务数/完成数/预估时长与「超出」徽标，不可用星期置灰、今天高亮；点击选日展示当日任务（完成/编辑/延期/删除/添加），支持历史日期补录（FR-3.6）。
- **任务延期**：`Tasks.original_planned_date` 记录原计划日期（FR-3.3 验收）；延期保留内容、归属与预估时长。
- **次日未完成任务集中确认（FR-3.7）**：今天页横幅提示昨日及更早未完成任务，支持批量延期至下一可用日/指定日期/保留原日期（不自动改计划）。
- **计划偏好设置**：每日可用时长（小时/分钟步进器，点击数字可直接输入）与每周可用日；保存后今天页负载/日历/快捷延期即时生效。
- **目标详情负载区（FR-5.3/5.4）**：剩余任务时长、剩余可用天数、建议日均时长；建议日均超可用时长时显示计划风险与建议（只建议不自动改，FR-5.5）。
- **数据库 schema v2**：`Settings` 计划偏好表、`Tasks.original_planned_date`，含迁移与失败回滚测试。

### 新增（迭代增强）

- **JSON 导入任务**（批量添加升级版）：按科目分组与未分类任务隔离；自动校验（JSON 合法性、日期不得早于今天、非法日期拦截），错误带位置逐条列出；目标下不存在的科目自动创建；单事务写入。
- **JSON 导入替换/合并**：替换模式将当前任务归档到「历史任务」（可手动恢复），合并模式追加到现有计划；替换前二次确认。
- **历史任务区**：目标详情底部展示 JSON 替换时归档保留的旧任务，支持恢复回当前计划（schema v3 `Tasks.archived_at`）。
- **重复任务（FR-4，P1 提前交付）**：可扩展规则引擎（RecurrenceRuleHandler + Registry），内置每天/每周指定星期/每隔 N 天/间隔序列（默认艾宾浩斯 1,2,4,7,15,30）；「模板 + 实例」模型，仅预生成未来 30 天实例并滚动生成；支持可选结束日期、编辑规则（仅修改模板/仅修改未来实例二选一）、停止重复（历史实例保留）；schema v4。
- **删除目标后跨页刷新**：目标删除/完成/归档后，今天页、日历、详情与历史任务缓存统一刷新（FR-3 验收）。

### 修复

- 目标详情页无返回键：导航由 `go` 改为 `push`，恢复 AppBar 返回箭头（PR #5）。
- 编辑目标后详情页不刷新：编辑保存时同步刷新列表与详情数据源（PR #5）。
- 未分类任务区补充用途引导文案（科目复习/复盘、考研报名等），空态文案引导创建（PR #9）。
- 删除目标后今天页/日历仍显示其任务：改为 family 级统一刷新任务缓存。
- 日历「超出」数值跨页刷新不一致：今天页操作后同步刷新日历月数据。

### 修复（代码审查：P0 崩溃 + P1 功能 + P2 健壮性）

**崩溃级（P0）**
- 创建目标不选截止日期直接崩溃（`_deadline!` 空值解包）：截止日期改为 `FormField` 必填校验，未选择时展示错误而非崩溃。
- 删除被重复任务模板引用的科目触发外键约束异常：删除科目时同步置空模板的 `subject_id`。
- 编辑规则类型未注册的重复模板崩溃（SegmentedButton 断言）：未注册类型回退到默认规则。

**功能缺陷（P1）**
- 编辑任务无法清空备注：`TaskRepository.update` 的 `note` 改为 `Value<String?>?`，空备注显式置空落库。
- 重复任务编辑框的标题/科目/预估时长/起始日期此前不生效（`updateRule` 只更新规则）：现在写入模板，未来实例重生成时使用更新后的基础信息。
- 今天页逾期横幅被空态遮蔽：无活跃目标且无今日任务但有逾期任务时，横幅与任务区正常展示。
- 今天页空态快捷添加后新任务不出现：改为等待对话框保存完成后再刷新缓存。
- 任务表单/批量表单保存后只刷新本目标列表，今天页/日历/逾期横幅缓存陈旧：补全跨页 invalidate。

**健壮性（P2）**
- JSON 导入自动新建科目后科目列表不刷新：补 `subjectListProvider` invalidate。
- 删除目标后重复模板缓存未失效：补 `recurrenceTemplatesProvider` invalidate。
- 重复规则参数类型不安全（`as int` 遇非 int 抛 TypeError）：handler 改类型守卫，注册表外层 try/catch 兜底。
- `SettingsRepository.get()` 并发首次调用触发 UNIQUE 约束竞态：惰性 seed 改 `insertOrIgnore`。
- JSON 导入超长标题/科目名触发底层数据库异常：解析层校验标题 ≤200、科目名 ≤100。
- **删除的重复任务实例会「复活」**（滚动生成时按规则重新出现）：schema v5 为模板增加 `deleted_instance_dates` 墓碑列，删除实例时记录日期，`generateDue`/`updateRule` 跳过墓碑日期；迁移改为 drift step-by-step（各版本快照建表，杜绝累积迁移重复建列）。

### 已知限制

- **检查项与里程碑（P1）**：任务检查项（FR-4.1）、目标里程碑（FR-2）未实现。
- **进度与数据保障（M3）**：统计、热力图、手动备份/恢复、托盘与窗口恢复、性能基线未实现。
- **AI 计划草稿（P1 / M5）**：未实现。
- **重复任务（已交付）**：艾宾浩斯等间隔序列按天计算（同一天不产生多条同内容实例）；「仅修改模板」时已生成实例不自动重排；JSON 替换导入会停用该目标的重复模板。
- **数据存储位置**：数据库位于系统文档目录（`%USERPROFILE%\Documents\timecalc.sqlite`），可配置备份路径属 M3 范围。
