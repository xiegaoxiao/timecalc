# M1 里程碑验收记录：数据与目标闭环

> 里程碑：M1 数据与目标闭环（PRD §12）
>
> 验收日期：2026-08-05
>
> 验收方式：对照 [开发检查清单](checklists.md) §11 M1 门禁 + [SOP](development-sop.md) 附录 D 流程门禁，逐项记录证据。
>
> 范围锁定：本里程碑仅交付 M1 内容（PRD §12），未启动 M2 任务；P1 项均未纳入（未选中的 P1 标记为「不适用」，不阻塞 M1）。

## 1. 流程门禁（SOP 附录 D）

| 检查项 | 结果 | 证据 |
|---|---|---|
| S0：新依赖已登记且许可证合规 | ✅ | SOP 附录 C 登记 7 项依赖（drift、drift_flutter、drift_dev、build_runner、flutter_riverpod、go_router、intl、path_provider），均为 MIT/BSD-3-Clause/SDK |
| S1：功能已映射到 FR 与 P 级 | ✅ | PR #2/#3/#4 描述均标注关联 FR；范围未超 M1 |
| S2：范围未超出 PRD 对应里程碑边界 | ✅ | 交付内容与 M1 定义一致；日历/延期/负载（M2）、里程碑（P1）、热力图/备份（M3）均未启动 |
| S3：schema 变更含 migration 与测试 | ✅ | schema v1 空迁移 + `migration_test.dart`；升级前备份/快照正式机制随数据演进里程碑落地（PRD §12 说明） |
| S4：事务/隐私硬规则落实 | ✅ | 删除目标级联事务、任务完成状态切换事务、科目删除时任务解除归属（代码审查 + 事务回滚测试）；无隐私数据写入日志 |
| S5：适用必测项与性能基线通过 | ✅ | 56 项测试全绿（明细见 §2）；M1 无性能基线要求（M3 起） |
| S6：审查清单通过 | ✅ | PR #1~#4 均经描述、验证方式、已知限制与第三方依赖说明后合并 |
| S7：退出条件逐项通过 | ✅ | 见 §2 |
| 备份恢复演练（M3 起） | N/A | M3 里程碑再执行 |

## 2. M1 门禁明细（checklists §11 M1）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 目标、科目、任务 CRUD 完成 | ✅ | Repository 测试 `goal_repository_test.dart` / `task_repository_test.dart`；Widget 测试 `goal_crud_widget_test.dart` / `task_crud_widget_test.dart` |
| 本地数据库与 migration 模板建立，migration 测试通过 | ✅ | `lib/core/database/`（drift schema v1）；`migration_test.dart` 4 项全绿 |
| 应用重启后数据持久化一致 | ✅ | `persistence_test.dart`：关闭后重开文件库，目标与任务数据一致 |
| 删除目标二次确认（含删除任务提示）通过 | ✅ | `goal_crud_widget_test.dart`「删除目标前二次确认并明确提示连带删除任务」；文案含「将删除……及其全部任务」 |
| 日期/时区关键边界测试（跨日、跨年、DST）通过 | ✅ | `countdown_service_test.dart`：跨日/跨年/闰年/DST 切换/非闰年 2 月 28 日边界，14 项全绿 |

## 3. Windows 手动冒烟（S7 数据演练）

1. `flutter build windows --debug` 构建成功（`build\windows\x64\runner\Debug\timecalc.exe`）。
2. 启动应用：进程正常运行，Dart VM 服务启动无异常。
3. 首次运行创建生产库 `%USERPROFILE%\Documents\timecalc.sqlite`（drift_flutter 默认目录）。
4. 以 drift 连接生产库验证：`goals`/`subjects`/`tasks` 三张表存在，可查询。
5. 关闭后再次启动：在已有数据库上正常启动，无迁移错误。

> 备注：正式安装路径与打包（MSIX/安装包）属 M4 发布候选范围，M1 仅验证开发构建链路。

## 4. 测试统计

- `flutter analyze`：0 问题。
- `flutter test`：56 项常驻测试全部通过（4 个 PR 累积）；另执行 1 次临时冒烟验证读取生产库（验证后移除，不常驻版本库）。
- 覆盖：migration 4、GoalRepository 7、TaskRepository 10、倒计时边界 14、时长校验 8、Widget 流程 11、重启持久化 1、骨架冒烟 1。

## 5. 交付物与合并记录

| PR | 分支 | 内容 | 状态 |
|---|---|---|---|
| #1 | chore/scaffold | Flutter Windows 工程 + 架构骨架 + 依赖 | 已合并 |
| #2 | feat/database | schema v1 + migration + Repository + CountdownService + 测试 | 已合并 |
| #3 | feat/goal-crud | 目标 CRUD + 删除二次确认 + 今天页倒计时 + Widget 测试 | 已合并 |
| #4 | feat/task-crud | 科目与任务 CRUD + 时长校验 + Widget 测试 | 已合并 |

## 6. 下一里程碑输入（M2 每日执行闭环）

- 今天页完整闭环：今日任务列表、完成/延期、快捷默认延期至下一可用日。
- 日历视图（FR-3.4）与「超出 X 分钟」负载提示（FR-3.5）。
- 负载计算（FR-5.2）与目标详情剩余工作量（FR-5.3）。
- M2 退出条件：用户可连续完成「查看今日任务 → 完成/延期 → 查看更新后进度」。

## 7. 遗留事项

- schema 升级前备份/快照与失败恢复的正式机制（migration v2 起落地）。
- 生产库路径为 drift_flutter 默认文档目录；M3 起引入可配置备份路径时统一处理。
- 手动冒烟后生产库 `timecalc.sqlite` 为开发验证数据，发布前不随应用分发。
