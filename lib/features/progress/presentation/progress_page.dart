import 'package:flutter/material.dart';

/// 进度页：M3 交付基础统计与热力图。
class ProgressPage extends StatelessWidget {
  const ProgressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('进度')),
      body: const Center(
        child: Text('进度页（M3 开发中）'),
      ),
    );
  }
}
